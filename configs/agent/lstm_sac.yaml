 # lstm_sac.yaml
# 为论文第 5 章 LSTM-SAC 预留的配置文件

# sequence_length：状态序列长度，即 LSTM 输入多少个连续状态。
sequence_length: 8

# lstm_hidden_dim：LSTM 隐藏状态维度。
lstm_hidden_dim: 256

# lstm_num_layers：LSTM 层数。
lstm_num_layers: 1

# 其余 SAC 参数后续可继承 configs/agent/sac.yaml。