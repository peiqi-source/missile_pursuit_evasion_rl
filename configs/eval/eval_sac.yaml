# eval_sac.yaml
# SAC 测试参数配置文件
# 单独测试某个模型时才需要

# eval_episodes：测试回合数。
eval_episodes: 10

# render：是否在测试时打印环境状态。
render: false

# checkpoint_path：待加载模型路径。
checkpoint_path: "experiments/sac_baseline/checkpoints/latest.pth"